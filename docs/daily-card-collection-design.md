# 每日集卡功能设计文档

## 目录
- [功能概述](#功能概述)
- [卡牌系统设计](#卡牌系统设计)
- [抽卡机制](#抽卡机制)
- [UI/UX设计](#uiux设计)
- [数据存储设计](#数据存储设计)
- [概率算法设计](#概率算法设计)
- [技术实现](#技术实现)
- [开发计划](#开发计划)

---

## 功能概述

### 功能定位
每日集卡是一个休闲抽卡功能，用户通过"摇一摇"或"签到"的方式，每日可抽取3张卡牌，收集中国传统元素和易经卦象。

### 核心特性
- ✨ 每日3次免费抽卡机会
- 🎴 两套卡牌主题：传统元素（52张）+ 易经卦象（64张）
- 💫 三种稀有度：R（普通）、SR（稀有）、SSR（超稀有）
- 📱 手机端优化：摇一摇抽卡 + 点击抽卡
- 🎯 集卡成就：收集进度、图鉴解锁
- 🔮 易经占卜：抽到的卦象可查看详细解释

---

## 卡牌系统设计

### 卡牌类型

#### 类型1：传统元素卡牌（52张）
基于扑克牌设计的52张中国传统元素卡牌，分为4个花色，每个花色13张：

**♠️ 黑桃 - 生肖系列（12张）**
| 序号 | 卡牌 | 中文名 | 英文名 | 五行 |
|------|------|--------|--------|------|
| 1 | 子鼠 | Rat | 子鼠 | 水 |
| 2 | 丑牛 | Ox | 丑牛 | 土 |
| 3 | 寅虎 | Tiger | 寅虎 | 木 |
| 4 | 卯兔 | Rabbit | 卯兔 | 木 |
| 5 | 辰龙 | Dragon | 辰龙 | 土 |
| 6 | 巳蛇 | Snake | 巳蛇 | 火 |
| 7 | 午马 | Horse | 午马 | 火 |
| 8 | 未羊 | Sheep | 未羊 | 土 |
| 9 | 申猴 | Monkey | 申猴 | 金 |
| 10 | 酉鸡 | Rooster | 酉鸡 | 金 |
| 11 | 戌狗 | Dog | 戌狗 | 土 |
| 12 | 亥猪 | Pig | 亥猪 | 水 |

**♥️ 红心 - 节气系列（12张）**
| 序号 | 卡牌 | 中文名 | 节气 | 季节 |
|------|------|--------|------|------|
| 1 | 立春 | Beginning of Spring | 立春 | 春 |
| 2 | 雨水 | Rain Water | 雨水 | 春 |
| 3 | 惊蛰 | Awakening of Insects | 惊蛰 | 春 |
| 4 | 春分 | Spring Equinox | 春分 | 春 |
| 5 | 清明 | Qingming | 清明 | 春 |
| 6 | 谷雨 | Grain Rain | 谷雨 | 春 |
| 7 | 立夏 | Beginning of Summer | 立夏 | 夏 |
| 8 | 小满 | Grain Full | 小满 | 夏 |
| 9 | 芒种 | Grain in Ear | 芒种 | 夏 |
| 10 | 夏至 | Summer Solstice | 夏至 | 夏 |
| 11 | 小暑 | Minor Heat | 小暑 | 夏 |
| 12 | 大暑 | Major Heat | 大暑 | 夏 |

**♦️ 方片 - 时辰系列（12张）**
| 序号 | 卡牌 | 时间段 | 中文名 | 卦象 |
|------|------|--------|------|------|
| 1 | 子时 | 23:00-01:00 | 子时 | 坎 |
| 2 | 丑时 | 01:00-03:00 | 丑时 | 坤 |
| 3 | 寅时 | 03:00-05:00 | 寅时 | 艮 |
| 4 | 卯时 | 05:00-07:00 | 卯时 | 艮 |
| 5 | 辰时 | 07:00-09:00 | 辰时 | 巽 |
| 6 | 巳时 | 09:00-11:00 | 巳时 | 离 |
| 7 | 午时 | 11:00-13:00 | 午时 | 离 |
| 8 | 未时 | 13:00-15:00 | 未时 | 坤 |
| 9 | 申时 | 15:00-17:00 | 申时 | 兑 |
| 10 | 酉时 | 17:00-19:00 | 酉时 | 兑 |
| 11 | 戌时 | 19:00-21:00 | 戌时 | 乾 |
| 12 | 亥时 | 21:00-23:00 | 亥时 | 坎 |

**♣️ 梅花 - 四季系列（12张）**
| 序号 | 卡牌 | 中文名 | 意象 | 五行 |
|------|------|--------|------|------|
| 1 | 春风 | Spring Wind | 春风 | 木 |
| 2 | 春雨 | Spring Rain | 春雨 | 水 |
| 3 | 夏雷 | Summer Thunder | 夏雷 | 火 |
| 4 | 夏雨 | Summer Rain | 夏雨 | 水 |
| 5 | 秋霜 | Autumn Frost | 秋霜 | 金 |
| 6 | 秋露 | Autumn Dew | 秋露 | 水 |
| 7 | 冬雪 | Winter Snow | 冬雪 | 水 |
| 8 | 冬霜 | Winter Frost | 冬霜 | 金 |
| 9 | 晨曦 | Morning Light | 晨曦 | 火 |
| 10 | 夕阳 | Sunset | 夕阳 | 火 |
| 11 | 朝阳 | Morning Sun | 朝阳 | 火 |
| 12 | 月光 | Moonlight | 月光 | 金 |

**补充：天干地支（12张）**
| 序号 | 卡牌 | 中文名 | 五行 | 方位 |
|------|------|--------|------|------|
| 1 | 甲子 | Jia Zi | 木 | 东 |
| 2 | 乙丑 | Yi Chou | 木 | 东北 |
| 3 | 丙寅 | Bing Yin | 火 | 东 |
| 4 | 丁卯 | Ding Mao | 火 | 东 |
| 5 | 戊辰 | Wu Chen | 土 | 东南 |
| 6 | 己巳 | Ji Si | 土 | 东南 |
| 7 | 庚午 | Geng Wu | 金 | 南 |
| 8 | 辛未 | Xin Wei | 金 | 南 |
| 9 | 壬申 | Ren Shen | 水 | 西南 |
| 10 | 癸酉 | Gui You | 水 | 西 |
| 11 | 甲戌 | Jia Xu | 木 | 西北 |
| 12 | 乙亥 | Yi Hai | 木 | 西北 |

**补充：五行元素（4张）**
| 序号 | 卡牌 | 中文名 | 特性 | 颜色 |
|------|------|--------|------|------|
| 1 | 金 | Metal | 坚固 | 白金 |
| 2 | 木 | Wood | 生长 | 翠绿 |
| 3 | 水 | Water | 流动 | 深蓝 |
| 4 | 火 | Fire | 燃烧 | 炽红 |
| 5 | 土 | Earth | 稳固 | 黄褐 |

> **注**：以上共52张卡牌，对应标准扑克牌数量

#### 类型2：易经卦象卡牌（64张）

**乾宫八卦（8卦）**
| 序号 | 卡牌 | 卦名 | 卦象 | 上卦 | 下卦 |
|------|------|------|------|------|------|
| 1 | 乾为天 | Qian | 乾 | 乾 | 乾 |
| 2 | 坤为地 | Kun | 坤 | 坤 | 坤 |
| 3 | 水雷屯 | Tun | 屯 | 坎 | 震 |
| 4 | 山水蒙 | Meng | 蒙 | 艮 | 坎 |
| 5 | 水天需 | Xu | 需 | 坎 | 乾 |
| 6 | 天水讼 | Song | 讼 | 乾 | 坎 |
| 7 | 地水师 | Shi | 师 | 坤 | 坎 |
| 8 | 水地比 | Bi | 比 | 坎 | 坤 |

**巽宫八卦（8卦）**
| 序号 | 卡牌 | 卦名 | 卦象 | 上卦 | 下卦 |
|------|------|------|------|------|------|
| 9 | 风天小畜 | Xiao Xu | 小畜 | 巽 | 乾 |
| 10 | 天泽履 | Lv | 履 | 乾 | 兑 |
| 11 | 地天泰 | Tai | 泰 | 坤 | 乾 |
| 12 | 天地否 | Pi | 否 | 乾 | 坤 |
| 13 | 天火同人 | Tong Ren | 同人 | 乾 | 离 |
| 14 | 火天大有 | Da You | 大有 | 离 | 乾 |
| 15 | 地山谦 | Qian | 谦 | 坤 | 艮 |
| 16 | 雷地豫 | Yu | 豫 | 震 | 坤 |

**坎宫八卦（8卦）**
| 序号 | 卡牌 | 卦名 | 卦象 | 上卦 | 下卦 |
|------|------|------|------|------|------|
| 17 | 泽雷随 | Sui | 随 | 兑 | 震 |
| 18 | 山风蛊 | Gu | 蛊 | 艮 | 巽 |
| 19 | 地泽临 | Lin | 临 | 坤 | 兑 |
| 20 | 风地观 | Guan | 观 | 巽 | 坤 |
| 21 | 火雷噬嗑 | Shi Ke | 噬嗑 | 离 | 震 |
| 22 | 山火贲 | Ben | 贲 | 艮 | 离 |
| 23 | 山地剥 | Bo | 剥 | 艮 | 坤 |
| 24 | 地雷复 | Fu | 复 | 坤 | 震 |

**艮宫八卦（8卦）**
| 序号 | 卡牌 | 卦名 | 卦象 | 上卦 | 下卦 |
|------|------|------|------|------|------|
| 25 | 天雷无妄 | Wu Wang | 无妄 | 乾 | 震 |
| 26 | 山天大畜 | Da Chu | 大畜 | 艮 | 乾 |
| 27 | 山雷颐 | Yi | 颐 | 艮 | 震 |
| 28 | 泽风大过 | Da Guo | 大过 | 兑 | 巽 |
| 29 | 坎为水 | Kan | 坎 | 坎 | 坎 |
| 30 | 离为火 | Li | 离 | 离 | 离 |
| 31 | 泽山咸 | Xian | 咸 | 兑 | 艮 |
| 32 | 雷风恒 | Heng | 恒 | 震 | 巽 |

**坤宫八卦（8卦）**
| 序号 | 卡牌 | 卦名 | 卦象 | 上卦 | 下卦 |
|------|------|------|------|------|------|
| 33 | 天山遁 | Dun | 遁 | 乾 | 艮 |
| 34 | 雷天大壮 | Da Zhuang | 大壮 | 震 | 乾 |
| 35 | 火地晋 | Jin | 晋 | 离 | 坤 |
| 36 | 地火明夷 | Ming Yi | 明夷 | 坤 | 离 |
| 37 | 风火家人 | Jia Ren | 家人 | 巽 | 离 |
| 38 | 火泽睽 | Kui | 睽 | 离 | 兑 |
| 39 | 水山蹇 | Jian | 蹇 | 坎 | 艮 |
| 40 | 雷水解 | Jie | 解 | 震 | 坎 |

**兑宫八卦（8卦）**
| 序号 | 卡牌 | 卦名 | 卦象 | 上卦 | 下卦 |
|------|------|------|------|------|------|
| 41 | 山泽损 | Sun | 损 | 艮 | 兑 |
| 42 | 风雷益 | Yi | 益 | 巽 | 震 |
| 43 | 泽天夬 | Gua | 夬 | 兑 | 乾 |
| 44 | 天风姤 | Gou | 姤 | 乾 | 巽 |
| 45 | 泽地萃 | Cui | 萃 | 兑 | 坤 |
| 46 | 地风升 | Sheng | 升 | 坤 | 巽 |
| 47 | 泽水困 | Kun | 困 | 兑 | 坎 |
| 48 | 水风井 | Jing | 井 | 坎 | 巽 |

**离宫八卦（8卦）**
| 序号 | 卡牌 | 卦名 | 卦象 | 上卦 | 下卦 |
|------|------|------|------|------|------|
| 49 | 泽火革 | Ge | 革 | 兑 | 离 |
| 50 | 火风鼎 | Ding | 鼎 | 离 | 巽 |
| 51 | 震为雷 | Zhen | 震 | 震 | 震 |
| 52 | 艮为山 | Gen | 艮 | 艮 | 艮 |
| 53 | 风山渐 | Jian | 渐 | 巽 | 艮 |
| 54 | 雷泽归妹 | Gui Mei | 归妹 | 震 | 兑 |
| 55 | 雷火丰 | Feng | 丰 | 震 | 离 |
| 56 | 火山旅 | Lv | 旅 | 离 | 艮 |

**震宫八卦（8卦）**
| 序号 | 卡牌 | 卦名 | 卦象 | 上卦 | 下卦 |
|------|------|------|------|------|------|
| 57 | 巽为风 | Xun | 巽 | 巽 | 巽 |
| 58 | 兑为泽 | Dui | 兑 | 兑 | 兑 |
| 59 | 风水涣 | Huan | 涣 | 巽 | 坎 |
| 60 | 水泽节 | Jie | 节 | 坎 | 兑 |
| 61 | 风泽中孚 | Zhong Fu | 中孚 | 巽 | 兑 |
| 62 | 雷山小过 | Xiao Guo | 小过 | 震 | 艮 |
| 63 | 水火既济 | Ji Ji | 既济 | 坎 | 离 |
| 64 | 火水未济 | Wei Ji | 未济 | 离 | 坎 |

### 稀有度系统

#### R卡 - 普通（Rare）
**概率：70%**
- **背景**：纯色渐变背景
- **边框**：细边框，银灰色
- **特效**：轻微光晕效果
- **动画**：淡入动画
- **收集难度**：⭐

**颜色方案**：
- 传统元素卡牌：蓝绿色渐变
- 易经卦象卡牌：蓝紫色渐变

#### SR卡 - 稀有（Super Rare）
**概率：25%**
- **背景**：渐变背景 + 纹理
- **边框**：中等宽度，金色
- **特效**：流动光效，边缘闪烁
- **动画**：缩放 + 旋转入场
- **收集难度**：⭐⭐⭐

**颜色方案**：
- 传统元素卡牌：紫金色渐变
- 易经卦象卡牌：橙金色渐变

#### SSR卡 - 超稀有（Super Super Rare）
**概率：5%**
- **背景**：动态粒子背景 + 彩虹渐变
- **边框**：粗边框，多色渐变
- **特效**：粒子爆炸 + 闪光效果
- **动画**：3D翻转 + 光芒爆发
- **声音**：特殊音效（可选）
- **收集难度**：⭐⭐⭐⭐⭐

**颜色方案**：
- 传统元素卡牌：红金渐变 + 彩虹光晕
- 易经卦象卡牌：金钻渐变 + 星空背景

### 卡牌视觉设计

#### 卡牌尺寸
- 手机端：120px × 180px
- 平板端：150px × 225px
- 桌面端：180px × 270px

#### 卡牌布局
```
┌─────────────────────────┐
│     [稀有度标识]       │ ← 顶部稀有度徽章
│                         │
│      [卡牌图标]        │ ← 中央主要图标/图案
│                         │
│      [卡牌名称]        │ ← 卡牌标题
│      [副标题]          │ ← 卡牌描述
│                         │
│    [稀有度边框]        │ ← 底部稀有度边框
└─────────────────────────┘
```

#### 设计元素
- **传统元素卡牌**：
  - 生肖系列：动物剪影 + 传统纹样
  - 节气系列：自然景观 + 诗意文字
  - 时辰系列：太阳/月亮 + 时辰钟表
  - 四季系列：季节景观 + 天气元素
  - 天干地支：太极图 + 八卦符号
  - 五行元素：元素符号 + 自然现象

- **易经卦象卡牌**：
  - 六爻符号（卦象）
  - 卦名（中文 + 拼音）
  - 卦辞摘要
  - 上卦/下卦标识

---

## 抽卡机制

### 每日限制
- **免费抽卡次数**：每日 3 次
- **次数重置**：每天 00:00 自动重置
- **抽卡方式**：
  1. 📱 摇一摇抽卡
  2. 👆 点击抽卡
  3. 📅 签到抽卡（与每日签到联动）

### 抽卡流程

#### 方式1：摇一摇抽卡
```
1. 用户打开抽卡页面
2. 手机竖直握持
3. 快速摇动手机
4. 触发抽卡动画
5. 显示抽卡结果
6. 自动保存到卡牌收藏
```

**技术实现**：
- 使用 DeviceMotion API 检测摇动
- 设置加速度阈值
- 防抖处理（2秒内只能抽一次）

#### 方式2：点击抽卡
```
1. 用户点击"抽卡"按钮
2. 显示卡牌翻转动画
3. 随机抽取卡牌
4. 展示抽卡结果
5. 添加到卡牌收藏
```

#### 方式3：签到抽卡
```
1. 用户完成每日签到
2. 自动触发一次抽卡
3. 消耗一次抽卡机会
4. 显示抽卡结果
```

### 概率算法

#### 基础概率分布
| 稀有度 | 概率 | 累积概率 |
|--------|------|----------|
| R卡 | 70% | 70% |
| SR卡 | 25% | 95% |
| SSR卡 | 5% | 100% |

#### 权重算法
```javascript
// 卡牌池权重配置
const CARD_WEIGHTS = {
  R: 70,
  SR: 25,
  SSR: 5
};

// 抽卡函数
function drawCard() {
  const totalWeight = Object.values(CARD_WEIGHTS).reduce((a, b) => a + b, 0);
  const random = Math.random() * totalWeight;

  let currentWeight = 0;
  for (const [rarity, weight] of Object.entries(CARD_WEIGHTS)) {
    currentWeight += weight;
    if (random < currentWeight) {
      return rarity;
    }
  }
  return 'R';
}
```

#### 重复卡牌处理
- 允许抽到重复卡牌
- 重复卡牌可以分解为"卡牌碎片"
- 卡牌碎片可合成更高稀有度的卡牌

### 保底机制（可选）
- **SR保底**：连续10次未抽到SR，第11次必出SR
- **SSR保底**：连续50次未抽到SSR，第51次必出SSR
- 保底计数跨天保留

---

## UI/UX设计

### 页面结构

#### 主页面布局
```
┌─────────────────────────────────┐
│  🎴 每日集卡                 │ ← 页面标题
│  今日剩余：3次                │ ← 抽卡次数显示
└─────────────────────────────────┘

┌─────────────────────────────────┐
│                         👆   │ ← 抽卡按钮/区域
│                     [卡背]   │ ← 卡牌正面（摇动时翻转）
│     摇一摇或点击抽卡         │ ← 提示文字
│                         👆   │
└─────────────────────────────────┘

┌─────────────────────────────────┐
│  最近抽到                    │ ← 最近抽到卡牌展示
│  [卡牌1] [卡牌2] [卡牌3]    │
└─────────────────────────────────┘

┌─────────────────────────────────┐
│  我的收藏  集卡进度  图鉴    │ ← 底部导航
│  56/116张                   │ ← 收集进度
└─────────────────────────────────┘
```

#### 抽卡结果页面
```
┌─────────────────────────────────┐
│  抽卡结果                    │
└─────────────────────────────────┘

┌─────────────────────────────────┐
│                             │
│     ★★ SSR ★★               │ ← 稀有度标签
│                             │
│        [卡牌图案]            │ ← 卡牌主图
│     乾为天                    │ ← 卡牌名称
│     Qian                     │ ← 英文名
│                             │
│   天行健，君子以自强不息       │ ← 卦辞/描述
│                             │
└─────────────────────────────────┘

┌─────────────────────────────────┐
│  [确定]  [分享]  [收藏]      │ ← 操作按钮
└─────────────────────────────────┘
```

#### 收藏页面
```
┌─────────────────────────────────┐
│  我的收藏              排序▼ │
│  筛选：[全部] [R] [SR] [SSR] │
└─────────────────────────────────┘

┌─────────────────────────────────┐
│  [卡牌1] [卡牌2] [卡牌3]    │
│  [卡牌4] [卡牌5] [卡牌6]    │
│  [卡牌7] [卡牌8] [卡牌9]    │
│  ...更多...                  │
└─────────────────────────────────┘

┌─────────────────────────────────┐
│  收集进度：56/116 (48.3%)    │
│  ██████████░░░░░░░░░        │ ← 进度条
└─────────────────────────────────┘
```

#### 图鉴页面
```
┌─────────────────────────────────┐
│  卡牌图鉴                    │
│  收集：56/116                │
└─────────────────────────────────┘

┌─────────────────────────────────┐
│  传统元素 (52张)             │
│  [✓] [✓] [✓] [✓] [✓]...   │ ← 已收集卡牌（高亮）
│  [✗] [✗] [✗] [✗] [✗]...   │ ← 未收集卡牌（灰暗）
└─────────────────────────────────┘

┌─────────────────────────────────┐
│  易经卦象 (64张)             │
│  [✓] [✓] [✓] [✓] [✓]...   │
│  [✗] [✗] [✗] [✗] [✗]...   │
└─────────────────────────────────┘
```

### 交互动画

#### 抽卡动画
1. **卡牌翻转**：3D翻转效果，从背面翻转到正面
2. **光效闪烁**：稀有度越高，光效越强
3. **粒子效果**：SSR卡牌触发粒子爆炸效果
4. **弹窗显示**：抽卡结果以弹窗形式展示

#### 收藏动画
1. **卡牌入场**：卡片从底部滑入
2. **稀有度高亮**：SR/SSR卡牌有特殊边框
3. **数量标识**：重复卡牌显示收集数量

### 响应式设计

#### 手机端（< 576px）
- 卡牌网格：2列布局
- 卡牌尺寸：120px × 180px
- 抽卡按钮：大按钮，易于点击
- 摇一摇提示：醒目显示

#### 平板端（576px - 992px）
- 卡牌网格：3-4列布局
- 卡牌尺寸：150px × 225px
- 抽卡区域：居中显示

#### 桌面端（> 992px）
- 卡牌网格：4-6列布局
- 卡牌尺寸：180px × 270px
- 抽卡区域：全屏居中

---

## 数据存储设计

### 数据结构

#### 用户卡牌数据
```json
{
  "userCards": {
    "traditional": {
      "rat": {
        "id": "rat",
        "name": "子鼠",
        "rarity": "R",
        "count": 3,
        "obtainedAt": ["2025-12-28", "2025-12-27", "2025-12-25"],
        "isNew": true
      },
      ...
    },
    "hexagram": {
      "qian": {
        "id": "qian",
        "name": "乾为天",
        "rarity": "SSR",
        "count": 1,
        "obtainedAt": ["2025-12-28"],
        "isNew": false
      },
      ...
    }
  },
  "dailyDraws": {
    "date": "2025-12-28",
    "remaining": 2,
    "draws": [
      {
        "cardId": "rat",
        "cardType": "traditional",
        "rarity": "R",
        "timestamp": "2025-12-28T10:30:00Z"
      },
      ...
    ]
  },
  "collectionProgress": {
    "traditional": {
      "total": 52,
      "collected": 30,
      "R": 20,
      "SR": 8,
      "SSR": 2
    },
    "hexagram": {
      "total": 64,
      "collected": 26,
      "R": 18,
      "SR": 7,
      "SSR": 1
    }
  }
}
```

#### 卡牌碎片数据
```json
{
  "cardFragments": {
    "R": 5,
    "SR": 3,
    "SSR": 1
  },
  "fragmentsHistory": [
    {
      "rarity": "SR",
      "source": "decompose",
      "amount": 2,
      "timestamp": "2025-12-28T10:30:00Z"
    }
  ]
}
```

#### 保底计数数据
```json
{
  "pitySystem": {
    "streak": {
      "sr": 5,
      "ssr": 42
    },
    "lastSR": null,
    "lastSSR": null
  }
}
```

### LocalStorage 键名

| 键名 | 数据类型 | 说明 |
|------|----------|------|
| `daily_cards_user_cards` | JSON | 用户卡牌收藏 |
| `daily_cards_draws` | JSON | 每日抽卡记录 |
| `daily_cards_fragments` | JSON | 卡牌碎片 |
| `daily_cards_pity` | JSON | 保底计数 |
| `daily_cards_collection_progress` | JSON | 收集进度 |
| `daily_cards_settings` | JSON | 用户设置 |

---

## 概率算法设计

### 稀有度概率计算

```javascript
/**
 * 根据权重随机抽取稀有度
 */
function drawRarity() {
  const weights = {
    'R': 70,      // 70%
    'SR': 25,     // 25%
    'SSR': 5      // 5%
  };

  const totalWeight = Object.values(weights).reduce((sum, weight) => sum + weight, 0);
  const random = Math.random() * totalWeight;
  let currentWeight = 0;

  for (const [rarity, weight] of Object.entries(weights)) {
    currentWeight += weight;
    if (random < currentWeight) {
      return rarity;
    }
  }

  return 'R';
}
```

### 卡牌池随机抽取

```javascript
/**
 * 从指定稀有度的卡牌池中随机抽取卡牌
 */
function drawCardFromPool(rarity, cardType) {
  const pool = CARD_POOLS[cardType][rarity];
  const randomIndex = Math.floor(Math.random() * pool.length);
  return pool[randomIndex];
}
```

### 保底系统实现

```javascript
/**
 * 保底抽卡逻辑
 */
function drawCardWithPity(pityData) {
  const rarity = drawRarity();

  // 检查是否触发SSR保底
  if (pityData.streak.ssr >= 50) {
    pityData.streak.ssr = 0;
    pityData.lastSSR = new Date().toISOString();
    return 'SSR';
  }

  // 检查是否触发SR保底
  if (pityData.streak.sr >= 10) {
    pityData.streak.sr = 0;
    pityData.lastSR = new Date().toISOString();
    return 'SR';
  }

  // 更新保底计数
  if (rarity === 'SSR') {
    pityData.streak.ssr = 0;
    pityData.streak.sr = 0;
    pityData.lastSSR = new Date().toISOString();
  } else if (rarity === 'SR') {
    pityData.streak.sr = 0;
    pityData.lastSR = new Date().toISOString();
  } else {
    pityData.streak.sr++;
    pityData.streak.ssr++;
  }

  return rarity;
}
```

### 连续抽卡概率调整（可选）

```javascript
/**
 * 连续抽卡概率调整
 * 每日第1次抽卡SSR概率提升到10%
 * 第2次5%，第3次正常5%
 */
function getAdjustedProbability(drawCount) {
  if (drawCount === 1) {
    return {
      'R': 65,
      'SR': 25,
      'SSR': 10
    };
  } else if (drawCount === 2) {
    return {
      'R': 70,
      'SR': 25,
      'SSR': 5
    };
  }
  return {
    'R': 70,
    'SR': 25,
    'SSR': 5
  };
}
```

---

## 技术实现

### 文件结构

```
frontend/src/
├── pages/
│   └── DailyCardPage.js              # 每日集卡主页面
├── components/
│   ├── dailyCards/
│   │   ├── CardDraw.js              # 抽卡组件
│   │   ├── CardResult.js            # 抽卡结果展示
│   │   ├── CardDisplay.js           # 卡牌展示组件
│   │   ├── CardCollection.js         # 收藏页面
│   │   ├── CardGallery.js           # 图鉴页面
│   │   ├── CardDetail.js            # 卡牌详情
│   │   ├── ShakeDetector.js         # 摇一摇检测
│   │   └── CardBack.js             # 卡背组件
│   └── dashboard/
│       └── FeatureCards.js          # 功能卡片（添加集卡卡片）
├── utils/
│   ├── cardConfig.js                # 卡牌配置数据
│   ├── cardProbability.js            # 概率算法
│   ├── cardStorage.js              # 卡牌存储管理
│   ├── cardPitySystem.js           # 保底系统
│   └── shakeUtils.js              # 摇一摇工具
└── styles/
    ├── dailyCards.css               # 集卡页面样式
    └── cardAnimations.css           # 卡牌动画
```

### 核心组件实现

#### 1. CardDraw.js - 抽卡组件
```jsx
const CardDraw = ({ onDraw, remaining }) => {
  const [isShaking, setIsShaking] = useState(false);
  const [isDrawing, setIsDrawing] = useState(false);

  // 摇一摇检测
  useEffect(() => {
    if (!window.DeviceMotionEvent) return;

    const handleShake = (event) => {
      const acceleration = event.accelerationIncludingGravity;
      const shakeThreshold = 15;

      if (!acceleration.x || !acceleration.y || !acceleration.z) return;

      const totalAcceleration =
        Math.abs(acceleration.x) +
        Math.abs(acceleration.y) +
        Math.abs(acceleration.z);

      if (totalAcceleration > shakeThreshold && !isShaking && remaining > 0) {
        setIsShaking(true);
        setTimeout(() => {
          setIsShaking(false);
          handleDraw();
        }, 1000);
      }
    };

    window.addEventListener('devicemotion', handleShake);
    return () => window.removeEventListener('devicemotion', handleShake);
  }, [isShaking, remaining]);

  const handleDraw = () => {
    if (remaining <= 0) {
      alert('今日抽卡次数已用完，明天再来吧！');
      return;
    }

    setIsDrawing(true);
    setTimeout(() => {
      onDraw();
      setIsDrawing(false);
    }, 1500);
  };

  return (
    <div className="card-draw-container">
      <div className={`card-back ${isDrawing ? 'drawing' : ''}`}>
        <div className="card-pattern">🎴</div>
        <div className="draw-hint">
          {isShaking ? '正在抽卡...' : '摇一摇或点击抽卡'}
        </div>
      </div>
      <button
        className="draw-button"
        onClick={handleDraw}
        disabled={remaining <= 0 || isDrawing}
      >
        {isDrawing ? '抽卡中...' : `抽卡 (${remaining}/3)`}
      </button>
    </div>
  );
};
```

#### 2. CardResult.js - 抽卡结果
```jsx
const CardResult = ({ card, onClose, onShare, onCollect }) => {
  const { name, rarity, description, image } = card;

  const rarityConfig = {
    'R': { class: 'rare-r', label: 'R', color: '#94a3b8' },
    'SR': { class: 'rare-sr', label: 'SR', color: '#fbbf24' },
    'SSR': { class: 'rare-ssr', label: 'SSR', color: '#f59e0b' }
  };

  const config = rarityConfig[rarity];

  return (
    <div className="card-result-modal">
      <div className={`card-result-content ${config.class}`}>
        <div className="rarity-badge" style={{ background: config.color }}>
          ★★ {config.label} ★★
        </div>

        <div className="card-display">
          <img src={image} alt={name} className="card-image" />
          <div className="card-info">
            <h3 className="card-name">{name}</h3>
            <p className="card-description">{description}</p>
          </div>
        </div>

        <div className="card-actions">
          <button className="action-button" onClick={onClose}>
            确定
          </button>
          <button className="action-button" onClick={onShare}>
            分享
          </button>
          <button className="action-button primary" onClick={onCollect}>
            收藏
          </button>
        </div>
      </div>
    </div>
  );
};
```

#### 3. CardCollection.js - 收藏页面
```jsx
const CardCollection = () => {
  const [cards, setCards] = useState([]);
  const [filter, setFilter] = useState('all');
  const [sort, setSort] = useState('obtained');

  // 加载用户卡牌
  useEffect(() => {
    const userCards = loadUserCards();
    setCards(userCards);
  }, []);

  // 筛选和排序
  const filteredCards = cards
    .filter(card => filter === 'all' || card.rarity === filter)
    .sort((a, b) => {
      if (sort === 'obtained') {
        return new Date(b.obtainedAt) - new Date(a.obtainedAt);
      } else if (sort === 'rarity') {
        const rarityOrder = { 'SSR': 3, 'SR': 2, 'R': 1 };
        return rarityOrder[b.rarity] - rarityOrder[a.rarity];
      }
      return 0;
    });

  return (
    <div className="card-collection">
      <div className="collection-header">
        <h2>我的收藏</h2>
        <div className="collection-filters">
          <select onChange={(e) => setFilter(e.target.value)}>
            <option value="all">全部</option>
            <option value="R">R卡</option>
            <option value="SR">SR卡</option>
            <option value="SSR">SSR卡</option>
          </select>
          <select onChange={(e) => setSort(e.target.value)}>
            <option value="obtained">按获得时间</option>
            <option value="rarity">按稀有度</option>
          </select>
        </div>
      </div>

      <div className="collection-progress">
        <div className="progress-bar">
          <div className="progress-fill" style={{ width: '48.3%' }}></div>
        </div>
        <div className="progress-text">56/116张 (48.3%)</div>
      </div>

      <div className="cards-grid">
        {filteredCards.map(card => (
          <CardDisplay key={card.id} card={card} />
        ))}
      </div>
    </div>
  );
};
```

### 卡牌配置数据

```javascript
// cardConfig.js
export const TRADITIONAL_CARDS = [
  {
    id: 'rat',
    name: '子鼠',
    englishName: 'Rat',
    type: 'zodiac',
    suit: 'spade',
    rarity: 'R',
    element: 'water',
    description: '聪明机智，灵活应变',
    image: '/images/cards/rat.png'
  },
  // ... 其他51张卡牌
];

export const HEXAGRAM_CARDS = [
  {
    id: 'qian',
    name: '乾为天',
    englishName: 'Qian',
    type: 'hexagram',
    rarity: 'SSR',
    upperTrigram: '乾',
    lowerTrigram: '乾',
    description: '天行健，君子以自强不息',
    image: '/images/cards/qian.png'
  },
  // ... 其他63张卦象
];

export const CARD_POOLS = {
  traditional: {
    'R': TRADITIONAL_CARDS.filter(c => c.rarity === 'R'),
    'SR': TRADITIONAL_CARDS.filter(c => c.rarity === 'SR'),
    'SSR': TRADITIONAL_CARDS.filter(c => c.rarity === 'SSR')
  },
  hexagram: {
    'R': HEXAGRAM_CARDS.filter(c => c.rarity === 'R'),
    'SR': HEXAGRAM_CARDS.filter(c => c.rarity === 'SR'),
    'SSR': HEXAGRAM_CARDS.filter(c => c.rarity === 'SSR')
  }
};
```

### 样式设计

```css
/* dailyCards.css */

.card-draw-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 20px;
  padding: 40px 20px;
}

.card-back {
  width: 180px;
  height: 270px;
  border-radius: 12px;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  transition: all 0.3s;
  position: relative;
  overflow: hidden;
}

.card-back.drawing {
  animation: flipCard 1.5s ease-in-out;
}

.card-back:hover {
  transform: scale(1.05);
  box-shadow: 0 10px 30px rgba(102, 126, 234, 0.4);
}

@keyframes flipCard {
  0% { transform: rotateY(0deg); }
  50% { transform: rotateY(90deg); }
  100% { transform: rotateY(0deg); }
}

/* 稀有度样式 */
.rare-r {
  background: linear-gradient(135deg, #64748b 0%, #94a3b8 100%);
  border: 2px solid #94a3b8;
}

.rare-sr {
  background: linear-gradient(135deg, #92400e 0%, #f59e0b 100%);
  border: 3px solid #fbbf24;
  box-shadow: 0 0 20px rgba(251, 191, 36, 0.5);
}

.rare-ssr {
  background: linear-gradient(135deg, #7c2d12 0%, #f97316 50%, #fbbf24 100%);
  border: 4px solid #fbbf24;
  box-shadow: 0 0 30px rgba(251, 191, 36, 0.8);
  animation: pulse 2s infinite;
}

@keyframes pulse {
  0%, 100% { box-shadow: 0 0 20px rgba(251, 191, 36, 0.6); }
  50% { box-shadow: 0 0 40px rgba(251, 191, 36, 1); }
}

/* 卡牌网格 */
.cards-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
  padding: 20px;
}

@media (max-width: 768px) {
  .cards-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 12px;
  }
}

/* 进度条 */
.progress-bar {
  width: 100%;
  height: 8px;
  background: #e5e7eb;
  border-radius: 4px;
  overflow: hidden;
}

.progress-fill {
  height: 100%;
  background: linear-gradient(90deg, #667eea 0%, #764ba2 100%);
  transition: width 0.3s;
}
```

### 集成到功能入口

在 `FeatureCards.js` 中添加：

```javascript
/**
 * 每日集卡组件
 */
const DailyCardCard = () => {
  return (
    <FeatureCard
      title="每日集卡"
      description="摇一摇收集精美卡牌"
      icon="sparkles"
      color="#8b5cf6"
      route="/daily-cards"
    />
  );
};

export {
  // ... 其他卡片
  DailyCardCard
};
```

在 `DashboardPage.js` 中导入和使用：

```javascript
import {
  // ... 其他导入
  DailyCardCard
} from '../components/dashboard/FeatureCards';

// 在功能网格中添加
<DailyCardCard />
```

---

## 开发计划

### 第一阶段：基础功能（Week 1）
- [x] 设计文档编写
- [ ] 卡牌数据配置（52+64张）
- [ ] 抽卡基础逻辑
- [ ] 卡牌展示组件
- [ ] 数据存储实现
- [ ] 集成到功能入口

### 第二阶段：抽卡体验（Week 2）
- [ ] 摇一摇功能实现
- [ ] 抽卡动画效果
- [ ] 稀有度视觉区分
- [ ] 抽卡结果展示
- [ ] 每日次数管理

### 第三阶段：收藏系统（Week 3）
- [ ] 收藏页面开发
- [ ] 卡牌筛选和排序
- [ ] 收集进度显示
- [ ] 重复卡牌处理
- [ ] 卡牌碎片系统

### 第四阶段：图鉴系统（Week 4）
- [ ] 图鉴页面开发
- [ ] 卡牌详情展示
- [ ] 收集进度统计
- [ ] 成就系统（可选）
- [ ] 分享功能

### 第五阶段：优化完善（Week 5）
- [ ] 性能优化
- [ ] 动画优化
- [ ] 响应式优化
- [ ] 错误处理
- [ ] 测试和修复

---

## 注意事项

### 兼容性
- 摇一摇功能需要设备支持 DeviceMotion API
- 部分浏览器需要用户授权才能访问加速度传感器
- 提供点击抽卡作为备选方案

### 性能
- 卡牌图片使用懒加载
- 抽卡动画使用 CSS transform
- 大量卡牌使用虚拟滚动
- LocalStorage 数据定期清理

### 用户体验
- 每日首次抽卡提升SSR概率（增加期待感）
- 新获得的卡牌标识
- 收集完成庆祝动画
- 清晰的稀有度标识

### 数据安全
- 所有数据保存在本地
- 定期备份机制（可选）
- 数据导出功能（可选）
- 防止数据丢失

---

## 总结

每日集卡功能结合了中国传统文化和现代抽卡玩法，具有以下特点：

1. **文化内涵**：传统元素 + 易经卦象，传播中国文化
2. **趣味性**：摇一摇抽卡，手机端原生体验
3. **收集感**：116张卡牌，重复抽卡，碎片合成
4. **成长性**：收集进度，稀有度递进，保底机制
5. **社交性**：分享功能，展示收藏
6. **技术性**：摇一摇检测，概率算法，本地存储

该功能能够增强用户粘性，提升日活跃度，同时传播中国传统文化。
